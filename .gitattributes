# NOTE: this pattern doesn't match anything inside this repo (db/init/ lives in campus-backend,
# a sibling clone — see repo-split-plan.md) and appears to have been a no-op even before the
# SQL Server migration removed the last *.sh file there (db/init/03_create_app_role.sh -> .sql).
# Left as a documented no-op rather than silently deleted, since removing it wasn't required by
# this change and it's pre-existing, cross-repo drift from the original split, not something
# this migration caused.
db/init/*.sh text eol=lf
